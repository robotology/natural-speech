! feature extraction

! train.sh

[-e env] or "source env" (-> possible to modify some parameters then)
feat and dot files

!! environment files
HOME=... -> to modify
CTX_EXP = {wi | cross}
MODEL_START = {flat | timit}
